  <div class="test">
    <div class="row">
      <div class="col-md-12">
        <% flash.each do |name, message| %>
        <div class="alert <%= bootstrap_class_for(name)%>">
          <%= message %>
        </div>
        <% end %>
      </div>
    </div>
  </div>
